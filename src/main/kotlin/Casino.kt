// Definition der offenen Klasse Casino mit dem Parameter tableNumber
open class Casino(val tableNumber: Int) {
    /* Dies ist der Konstruktor der Klasse Casino.
       Er erhält einen Parameter tableNumber, der beim Erstellen von Casino-Objekten übergeben wird. */

    // Hier beginnt der Konstruktor-Initialisierungsblock (init-Block).
    /*init {
        // Dieser Block wird ausgeführt, wenn ein Casino-Objekt erstellt wird.
        println("Dieser Tisch wurde im Casino initialisiert und hat die Nummer $tableNumber")
        // Hier wird eine Ausgabe in der Konsole gemacht, die die Tischnummer enthält.
    }*/
    // Ende des init-Blocks.
}
